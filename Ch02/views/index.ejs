<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>index</title>
  </head>
  <body>
    <h3>Ch03. Express 실습</h3>
    <h4>링크</h4>
    <a href="/sub/hello">Hello</a>
    <a href="/sub/welcome">Welcome</a>
    <a href="/sub/greeting">Greeting</a>

    <h4>출력</h4>
    <p>
      tit1 : <%= tit1 %>
      <br />
      tit2 : <%= tit2 %>
    </p>
    <p>
      uid : <%= user.uid %>
      <br />
      name : <%= user.name %>
      <br />
      hp : <%= user.hp %>
      <br />
      age : <%= user.age %>
    </p>

    <h4>조건문</h4>

    <h4>if</h4>
    <% if(result1){ %>
    <p>result1은 True</p>
    <% } %>

    <h4>if else</h4>
    <% if(result2){ %>
    <p>result1은 True</p>
    <% } else { %>
    <p>result2 는 False</p>
    <% } %>

    <h4>if else if</h4>
    <% if(score > 90){ %>
    <p>A</p>
    <% } else if(score > 80) { %>
    <p>B</p>
    <% } else if(score > 70) { %>
    <p>C</p>
    <% } else if(score > 60) { %>
    <p>D</p>
    <% } else {%>
    <p>F</p>
    <% }%>

    <h4>반복문</h4>
    <table border="1">
      <tr>
        <td>아이디</td>
        <td>이름</td>
        <td>휴대폰</td>
        <td>나이</td>
      </tr>
      <% for(const user of users){ %>
      <tr>
        <td><%= user.uid %></td>
        <td><%= user.name %></td>
        <td><%= user.hp %></td>
        <td><%= user.age %></td>
      </tr>
      <% } %>
    </table>
  </body>
</html>
